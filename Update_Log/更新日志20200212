1、采用在界面加入顶层Tab分页的方法集成BPA仿真功能，原因如下：
    1）数据格式不同
    2）更直观的操作

2、在“调用PSSE”页下加入第一子页“功能执行”，“功能执行”页下包括该环境下的常用功能，具体功能模块正在搭建。

3、在“调用BPA”页下加入第一子页“功能执行”，“功能执行”页下包括该环境下的常用功能，具体功能模块正在搭建。

3、函数位置顺序的整体思路：前半部分为与PSSE相关的程序，后半部分为与BPA相关的程序。函数中若调用函数，被调用函数则排列在其后。

4、对BPA提取数据的处理代码放在BPA的数据提取模块中（待写）

5、BPA数据分类以提取后的数据分类为基本依据，准备用表格形式展示。